<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="User">

<select id="idcheck" parameterType="java.lang.String" resultType="java.lang.Integer">
	select count(*)
	from users
	where user_id=#{user_id}
</select>

<insert id="addmember" parameterType="ssg.com.a.dto.UserDto">
	insert into users(user_id, password, user_name, phone_number, nick_name, email, auth)
	values(#{user_id}, #{password}, #{user_name}, #{phone_number}, #{nick_name}, #{email}, 3)
</insert>

<select id="login" parameterType="ssg.com.a.dto.UserDto" 
					resultType="ssg.com.a.dto.UserDto">
	select user_id, user_name, email, auth
	from users
	where user_id=#{user_id} and password=#{password}
</select>


</mapper>





