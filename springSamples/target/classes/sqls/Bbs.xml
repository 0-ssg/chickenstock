<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="Bbs">

<select id="bbslist" parameterType="ssg.com.a.dto.BbsParam" resultType="ssg.com.a.dto.BbsDto">
	select seq, id, ref, step, depth, title, content, wdate, del, readcount
	from bbs
	<if test="choice != null and choice != '' and search != null and search != '' ">
		<if test="choice == 'title'">					<!-- del=0 : 삭제된 글 검색되지 않도록  -->
			<!-- title안에서 search(단어)가 해당되는 부분 -->
			where title like concat('%', #{search}, '%') and del = 0
		</if>
		<if test="choice == 'content'">
			<!-- content안에서 search(단어)가 해당되는 부분 -->
			where content like concat('%', #{search}, '%') and del = 0
		</if>
		<if test="choice == 'writer'">
			<!-- 작성자(id)중 search(단어)가 해당되는 부분 -->
			where id=#{search} and del = 0
		</if> 
	</if>
	order by ref desc, step asc
	limit ${(pageNumber*10)}, 10
	
</select>

<select id="getAllBbs" parameterType="ssg.com.a.dto.BbsParam" resultType="java.lang.Integer">
	select count(*) 
	from bbs
	<if test="choice != null and choice != '' and search != null and search != '' ">
		<if test="choice == 'title'">					<!-- del=0 : 삭제된 글 검색되지 않도록  -->
			<!-- title안에서 search(단어)가 해당되는 부분 -->
			where title like concat('%', #{search}, '%') and del = 0
		</if>
		<if test="choice == 'content'">
			<!-- content안에서 search(단어)가 해당되는 부분 -->
			where content like concat('%', #{search}, '%') and del = 0
		</if>
		<if test="choice == 'writer'">
			<!-- 작성자(id)중 search(단어)가 해당되는 부분 -->
			where id=#{search} and del = 0
		</if> 
	</if>
</select>

<insert id="bbswrite" parameterType="ssg.com.a.dto.BbsDto">
	insert into bbs(id, ref, step, depth, title, content, wdate, del, readcount)
	values(#{id}, (select ifnull(max(ref), 0)+1 from bbs b), 0, 0, #{title}, #{content}, now(), 0, 0)
	
</insert>

<select id="bbsdetail" parameterType="java.lang.Integer" resultType="ssg.com.a.dto.BbsDto">
	select seq, id, ref, step, depth, title, content, wdate, del, readcount
	from bbs
	where seq=#{seq}
</select>

<select id="bbsget" parameterType="java.lang.Integer" resultType="ssg.com.a.dto.BbsDto">
	select seq, id, ref, step, depth, title, content, wdate, del, readcount 
	from bbs 
	where seq=#{seq}
</select>

<update id="bbsupdate" parameterType="ssg.com.a.dto.BbsDto" >
	update bbs 
	set title=#{title}, content=#{content} 
	where seq=#{seq}
</update>

<update id="bbsanswer" parameterType="ssg.com.a.dto.BbsDto">
	update bbs
	set step=step+1
	where ref=(select ref from (select ref from bbs a where seq=#{seq}) A)
		and step>(select step from (select step from bbs b where seq=#{seq}) B)
</update>

<insert id="bbsanswerAf" parameterType="ssg.com.a.dto.BbsDto">
	insert into bbs(id, ref, step, depth, title, content, wdate, del, readcount)
	values(#{id}, 
	(select ref from bbs a where seq=#{seq}),
	(select step from bbs b where seq=#{seq})+1,
	(select depth from bbs c where seq=#{seq})+1,
	#{title}, #{content}, now(), 0, 0) 
</insert>

<!-- 댓글(bbscomment) -->
<!-- 추가 -->
<insert id="commentWrite" parameterType="ssg.com.a.dto.BbsComment">
	insert into bbscomment(seq, id, content, wdate)
	values(#{seq}, #{id}, #{content}, now())
</insert>

<!-- 목록 -->
<select id="commentList" parameterType="Integer" resultType="ssg.com.a.dto.BbsComment">
	select seq, id, content, wdate
	from bbscomment
	where seq=#{seq}
	order by wdate desc
</select>
</mapper>


















